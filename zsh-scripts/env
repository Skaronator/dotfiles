#!/bin/bash

export HOMEBREW_NO_ENV_HINTS="false"

# Kubernetes
export KUBE_EDITOR="nano"
export DISABLE_TELEMETRY="true" # Disable k0s telemetry

# Python
export PATH="$HOME/.local/bin:$PATH"

# Primarly for TinyTeX
if [ -d "$HOME/bin" ]; then
  export PATH="$HOME/bin:$PATH"
fi
